# Top-level runtime dependencies (ranges). The exact, hashed lockfile the
# image installs is requirements.txt, generated from this file with:
#   uv pip compile requirements.in --python-version 3.12
#     --python-platform x86_64-manylinux_2_28 --generate-hashes -o requirements.txt
# Pin the FastMCP major line; uvicorn/starlette come in as FastMCP deps.
fastmcp>=4.0.9,<5
python-dotenv>=1.0
cryptography>=42          # Phase 2: encrypt stored Overleaf tokens at rest
azure-data-tables>=12.5   # Phase 2: persistent multi-tenant store
azure-identity>=1.19      # Phase 2: managed identity / Key Vault
aiohttp>=3.10             # async transport for the Azure SDK
stripe
PyMuPDF>=1.24             # render PDF pages to PNG for show_table / show_figure
